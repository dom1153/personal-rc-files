## Jekyll

Make sure ruby is installed:
```
[Windows instructions]
choco install ruby -y
```

Then install 
```
gem install bundler
gem install jekyll
```

Making the new site:
```
jekyll new myblog
cd myblog
jekyll serve
```