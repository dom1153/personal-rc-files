These are .desktop autostart files used with gnome
imwheel.desktop lauches imwheel, which sets mouse config for scrolling

Debian install location is ~/.config/autostart , or use debian 'tweaks' application
